{% extends 'base.html.twig' %}

{% block title %}{{ formation is defined ? 'Modifier' : 'Nouvelle' }} formation{% endblock %}

{% block body %}
<h1>{{ formation is defined ? 'Modifier' : 'Créer une nouvelle' }} formation</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}

    <button class="btn btn-success mt-3">{{ formation is defined ? 'Mettre à jour' : 'Créer' }}</button>
    <a href="{{ path('formations_list') }}" class="btn btn-secondary mt-3">Retour</a>
{{ form_end(form) }}
{% endblock %}