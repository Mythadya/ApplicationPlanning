{% extends 'base.html.twig' %}

{% block title %}Nouvelle interruption{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>➕ Nouvelle interruption</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success mt-3">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('interruptions_list') }}" class="btn btn-secondary mt-3">⬅ Retour</a>
</div>
{% endblock %}
