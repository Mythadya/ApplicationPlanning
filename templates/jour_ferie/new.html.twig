{% extends 'base.html.twig' %}

{% block title %}Ajouter un jour férié{% endblock %}

{% block body %}
<h1>Ajouter un jour férié</h1>

{{ form_start(form) }}
    {{ form_row(form.date) }}
    {{ form_row(form.nom) }}
    <button class="btn btn-primary">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('jours_feries_index') }}">Retour à la liste</a>
{% endblock %}
