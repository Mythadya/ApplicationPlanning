{% extends 'base.html.twig' %}

{% block title %}{{ form.vars.value.id ? 'Modifier' : 'Nouvelle' }} période en entreprise{% endblock %}

{% block body %}
<h1>{{ form.vars.value.id ? 'Modifier' : 'Ajouter' }} une période en entreprise</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-2">Valider</button>
{{ form_end(form) }}

<a href="{{ path('periode_entreprise_index') }}" class="btn btn-link mt-3">Retour</a>
{% endblock %}
